import React from 'react'
import Button from '@material-ui/core/Button'

const SubmitButton = () =>
  <Button
    type="submit"
    fullWidth
    variant="contained"
    color="primary"
  >
    Submit Form
  </Button>

export default SubmitButton
